export class Track {
  private id: number;
  private name: string;
  private duration: string;

  constructor( id: number, name: string, duration: string) {
    this.id = id;
    this.name = name;
    this.duration = duration;
  };

}
